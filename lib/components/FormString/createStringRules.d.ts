import { Rule } from 'rc-field-form/es/interface';
import { StringItem } from '../../types';
declare function createStringRules(languagePack: any, root: StringItem, required: boolean): Array<Rule>;
export default createStringRules;
