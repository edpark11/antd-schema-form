import { Rule } from "rc-field-form/es/interface";
import { ArrayItem } from "../../types";
declare function createArrayRules(languagePack: any, root: ArrayItem, required: boolean): Array<Rule>;
export default createArrayRules;
